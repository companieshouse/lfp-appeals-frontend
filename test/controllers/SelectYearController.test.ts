describe('SelectYearController', () => {
    it('should show an error if the penalty list is undefined');
    it('should render the radio buttons in the correct order');
    it('should show errors around the radio buttons if no option is selected on POST');
    it('should redirect to the review penalty page when penalty is selected');
});