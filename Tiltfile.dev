docker_build(
  ref = '169942020521.dkr.ecr.eu-west-1.amazonaws.com/local/lfp-appeals-frontend',
  context = '.',
  dockerfile = 'Dockerfile.dev',
  ssh = 'default',
  live_update = [
    sync(
      local_path = './src/views',
      remote_path = '/app/src/views'
    ),
    sync(
      local_path = './src',
      remote_path = '/app/src'
    )
  ]
)
